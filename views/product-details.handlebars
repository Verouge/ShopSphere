<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>{{product.name}}</title>
</head>

<body class="bg-gray-100">
    <div class="container mx-auto mt-10 px-4">
        <h1 class="text-3xl font-bold mb-8">{{product.name}}</h1>

        <div class="md:flex md:items-start mb-6">
            <div class="md:w-1/2">
                <!-- Slider or Image Gallery would be more suitable here, but for now, we'll display the first image as a large preview and others as thumbnails -->
                <img class="w-full mb-4 rounded-lg" src="{{product.imageUrls.[0]}}" alt="{{product.name}}">

                <div class="flex space-x-2 overflow-x-auto">
                    {{#each product.imageUrls}}
                    <img class="w-24 h-24 rounded-lg" src="{{this}}" alt="{{product.name}}">
                    {{/each}}
                </div>
            </div>

            <div class="md:w-1/2 md:pl-8">
                <p class="text-xl text-gray-600 mb-4">Price: ${{product.price}}</p>
                <p class="mb-2">Available: {{product.quantity}}</p>
                <p class="mb-2">Listed {{product.createdAt}} ago</p>
                <!-- Updated seller display -->
                <p class="mb-2">Seller's Store: 
                    <a href="/au/sellers/{{seller.id}}" class="text-blue-500 hover:underline">{{seller.username}}</a>
                    <!-- or if you don't have a route to individual sellers, you can just display the username without a link: {{seller.name}} -->
                </p>
                <p class="mb-2">Seller-Email: <span class="text-gray-600">{{seller.email}}</span></p>
                <button class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-6 rounded-full mt-4">
                    Add to Cart
                </button>
            </div>
        </div>

        <h2 class="text-2xl font-semibold mb-4">Product Description:</h2>
        <p class="text-gray-700 leading-relaxed mb-8">{{product.description}}</p>

    </div>
</body>

</html>
