<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet"> <!-- Adding the Tailwind CSS link -->
    <title>Seller Store</title>
</head>

<body class="bg-gray-100">

    <div class="container mx-auto mt-10">
            <h1 class="text-2xl font-bold mb-6">Products in {{seller.username}}</h1>


        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {{#each products}}
           
            <a href="/au/products/product-details/{{this.id}}" class="block product-card bg-white rounded-lg shadow-lg p-4">
                <img class="mx-auto mb-4 rounded-lg" src="{{this.imageUrls.[0]}}" alt="{{this.name}}" width="200" height="200">  <!-- Display the first image -->
                <h2 class="text-xl font-semibold">{{this.name}}</h2>
                <p class="text-gray-600 mt-2">Price: ${{this.price}}</p>
            </a>
            {{/each}}
        </div>
    </div>
    
</body>

</html>
